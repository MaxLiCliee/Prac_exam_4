#idndef GAME_HPP
#define GAME_HPP

include 

class game {
bool displayState() 
void movePlayer(int dx, int dy) ;
void printGrid()
}

Game(int width, int height, std::vector<std::tuple<int, int>> pitCoordinates)
// crying, not enough time:(
#endif